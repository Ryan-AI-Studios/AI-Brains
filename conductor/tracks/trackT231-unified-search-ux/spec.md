# T231 — Unified search UX defaults

- **Status:** 📋 Placeholder (plan-only until go)
- **Source:** Audit opportunity — best search UX is `sync query --pretty`; default `recall` is JSON/agent-oriented; dual mental model
- **Category:** UX / IA
- **Depends on:** T101 TTY pretty; T211 ranking; T217 FTS rescue

## Objective

One recommended “search” path for humans: pretty, scoped, ranked, with optional ledger pane. Reduce “which command?” friction.

## Draft options

| A | `recall` TTY default pretty + footer “also: sync query for ledger” |
| B | New alias `ai-brains search` → sync query pretty (or recall hybrid) |
| C | CAPABILITIES/WORKFLOWS “Start here” search recipe only (docs) |

Pick on go; prefer A+C before new noun.
