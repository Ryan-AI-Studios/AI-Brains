# T319 Plan — handle vs memory id (placeholder)

**Status:** **Placeholder.** Spec [spec.md](./spec.md). Full plan on `/plan-track T319`.
**Ledger (planning):** series DOCS `a6d3c404-1d64-4cba-a743-d75ac16c74cd`

## Phase 0 (on `/plan-track T319`)

- [ ] Re-read show miss paths + CLI-EXIT-CODES
- [ ] Confirm `QueryStore` memory EXISTS helper (no new projection)

## DoD (after full plan + go)

Pasting a live `memory_id` into `evidence show` names the namespace and points at recall. Still not H2.

## Isolation

No governed row fabrication. No `cargo install`. Never `git push origin main`.
