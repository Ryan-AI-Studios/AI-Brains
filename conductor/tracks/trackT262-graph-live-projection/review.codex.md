No product findings. CX1-P2 appears fixed.

The fix is in [graph.rs](C:/dev/AI-Brains/crates/ai-brains-cli/src/commands/graph.rs:82): `graph session` now classifies a missing graph node as vault-present if either `memory_exists` succeeds or `session_projection` contains the `session_id`, and the missing-node branch in [session()](C:/dev/AI-Brains/crates/ai-brains-cli/src/commands/graph.rs:427) uses that combined check. That closes the prior bug where a real session could fall through to F1b “not a vault memory id.” The regression test in [graph_live_projection.rs](C:/dev/AI-Brains/crates/ai-brains-cli/tests/graph_live_projection.rs:111) matches the intended failure mode exactly: it deletes only the session graph node, then asserts exit `0`, `graph rebuild` present, and no `not a vault memory id` / `graph update`.

Fresh sweep: neighbors and hierarchy still use `memory_exists` only on missing-node classification at [graph.rs](C:/dev/AI-Brains/crates/ai-brains-cli/src/commands/graph.rs:317) and [graph.rs](C:/dev/AI-Brains/crates/ai-brains-cli/src/commands/graph.rs:365), which matches the track’s stated non-goal for F18. I did not find a new regression in the T262 scope.

Residual risk: I could not rerun the hermetic tempdir-based tests in this read-only sandbox because execution is blocked by `%LOCALAPPDATA%\\Temp` write denial, so this re-review is source-verified rather than locally re-executed. Given the code path and test coverage present, I would move CX1-P2 in [review.md](C:/dev/AI-Brains/conductor/tracks/trackT262-graph-live-projection/review.md:31) from `fixed_pending_verification` to `verified_fixed`.