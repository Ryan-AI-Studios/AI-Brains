# T323 Plan — conclusion in-force (placeholder)

**Status:** **Placeholder.** Spec [spec.md](./spec.md). Full plan on `/plan-track T323`.
**Ledger (planning):** series DOCS `a6d3c404-1d64-4cba-a743-d75ac16c74cd`

## Phase 0 (on `/plan-track T323`)

- [ ] Open live conclusion projection + propose path
- [ ] If no successor chain: write decline ACs (honesty only) instead of a walker
- [ ] Reuse T311 test names pattern; do not share `governed_common.rs` hotspot

## DoD (after full plan + go)

Either a real in-force read **or** a written decline with evidence. No fake chain.

## Isolation

No H2. No `cargo install`. Never `git push origin main`.
