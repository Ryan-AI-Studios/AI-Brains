# T228 — Non-empty recall pretty Scope

- **Status:** 📋 Placeholder (plan-only until go)
- **Source:** T207 residual AC10 / audit — empty pretty has Scope; non-empty pretty does not
- **Category:** UX
- **Depends on:** T207; T214 `format_scope_line` SOOT

## Objective

Always print `Scope: …` on pretty recall (empty and non-empty), using same SOOT as preflight/memory list.

## Non-goals

Change JSON envelope.
