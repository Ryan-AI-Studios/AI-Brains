P1 - `DataEvent` can smuggle replication-control state through the inner JSON envelope. [replication_engine.rs](/abs/path/C:/dev/AI-Brains/crates/ai-brains-store/src/replication_engine.rs:816) blindly appends any decrypted `ai_brains_events::Envelope` via [event_store.rs](/abs/path/C:/dev/AI-Brains/crates/ai-brains-store/src/event_store.rs:170), which re-runs all projectors. That includes [ReplicationProjection](/abs/path/C:/dev/AI-Brains/crates/ai-brains-store/src/projections/replication.rs:1158), which trusts embedded membership fields such as `revoked_by_device_id`, `replication_event_id`, `signature_hex`, and `body_hex` from [payload.rs](/abs/path/C:/dev/AI-Brains/crates/ai-brains-events/src/payload.rs:698) without binding them to the verified outer `SignedEnvelope`. An enrolled peer can therefore tunnel `DeviceEnrolled` or `DeviceRevoked` through a `DataEvent` and mutate membership/control state outside the signed control path. That breaks the F9/F10 control-plane separation the track is enforcing.

P2 - apply/queue paths are not atomic, so failures can leave partially committed replication state. In [replication_engine.rs](/abs/path/C:/dev/AI-Brains/crates/ai-brains-store/src/replication_engine.rs:452), `apply_blob` projects side effects before inserting the envelope index and advancing the cursor. `project_data` commits an inner event in its own transaction via [event_store.rs](/abs/path/C:/dev/AI-Brains/crates/ai-brains-store/src/event_store.rs:170), and `project_control` mutates tombstones/CE/ack rows before outer indexing at [replication_engine.rs](/abs/path/C:/dev/AI-Brains/crates/ai-brains-store/src/replication_engine.rs:626). The local authority helpers do the same: [sign_and_queue_revoke](/abs/path/C:/dev/AI-Brains/crates/ai-brains-store/src/replication_engine.rs:1017) tombstones before `queue_control`, and [sign_and_queue_erasure_tombstone](/abs/path/C:/dev/AI-Brains/crates/ai-brains-store/src/replication_engine.rs:1045) destroys the key and writes pending ACK rows before the control/outbox write. If any later DB write fails, the command returns error after durable local mutation, and inbound apply can commit part of a relay envelope without its cursor/index state.

P3 - the new push/pull JSON contract is still not fully verified, and `pull`’s JSON field is semantically wrong. [replicate.rs](/abs/path/C:/dev/AI-Brains/crates/ai-brains-cli/src/commands/replicate.rs:281) emits `pulled_peers`, but the value is the count returned by `pull_all_peers()`, which is applied envelope count, not peer count. The CLI tests added in [device_replicate_cli.rs](/abs/path/C:/dev/AI-Brains/crates/ai-brains-cli/tests/device_replicate_cli.rs:160) only cover text mode; they do not exercise `--format json` for `push` or `pull`, which was the specific prior R1 CLI finding.

FAIL. The original R1 fixes for domain append and `GapSkipAudit` drain look present in code and tests, and the JSON branches were added, but the two issues above block clearance. I also could not independently rerun `cargo` or Ledgerful gates in this read-only sandbox, so I cannot issue a PASS on DoD verification from this environment.