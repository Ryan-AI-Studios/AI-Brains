# T294 Plan stub — context vault upsert

**Status:** **Pending** (Placeholder). Full F-list on `/plan-track 294`.
**Spec:** [spec.md](./spec.md)

- [ ] `/plan-track 294` before implement
- [ ] Hermetic .env id missing from vault — context upserts, bytes unchanged
- [ ] Manual DoD `context` + `project list` contains id; rebind dest exists
- [ ] Live leftover `--write` only if owner confirms

## DoD

- [ ] Dest id from `.env` is in the vault; `.env` not rewritten
